# Streamlit Attendance System

A simple attendance tracking app using Streamlit, Excel, and GitHub.

## Features

- Input name, status, login/logout time
- Save to Excel file (`attendance.xlsx`)
- View full attendance records
- GitHub version control

## How to Run

1. Clone repo
2. Run:

```bash
pip install -r requirements.txt
streamlit run attendance_app.py
```
